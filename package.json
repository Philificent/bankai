{
  "name": "bankai",
  "version": "0.1.0",
  "description": "The ultimate coding agent harness — owns the loop, swaps the model",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.23.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "dev": "pnpm build && node apps/cli/dist/index.js",
    "bankai": "node apps/cli/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^24.5.0",
    "typescript": "^5.9.2"
  }
}
